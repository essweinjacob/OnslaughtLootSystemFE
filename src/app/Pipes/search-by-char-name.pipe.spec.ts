import { SearchByCharNamePipe } from './search-by-char-name.pipe';

describe('SearchByCharNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByCharNamePipe();
    expect(pipe).toBeTruthy();
  });
});
